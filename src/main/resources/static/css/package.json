{
  "scripts": {
    "build:css": "npx tailwindcss -i input.css -o output.css --minify --watch",
    "prettier": "npx prettier --write ../../templates/*"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "tailwindcss": "^3.4.17"
  }
}
